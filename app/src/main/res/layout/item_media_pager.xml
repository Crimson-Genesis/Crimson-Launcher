<?xml version="1.0" encoding="utf-8"?>
<app.olauncher.helper.ZoomableFrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/zoomContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:focusable="true">

    <FrameLayout
        android:id="@+id/mediaContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/ivPreview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:scaleType="fitCenter"
            android:visibility="gone"
            android:contentDescription="@string/chat_media" />

        <VideoView
            android:id="@+id/vvPreview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:visibility="gone" />

        <LinearLayout
            android:id="@+id/llVideoControls"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_marginBottom="48dp"
            android:orientation="vertical"
            android:visibility="gone">

            <SeekBar
                android:id="@+id/sbVideoProgress"
                android:layout_width="match_parent"
                android:layout_height="32dp"
                android:layout_marginHorizontal="8dp"
                android:progressBackgroundTint="#40FFFFFF"
                android:progressTint="#FFFFFF"
                android:thumb="@drawable/white_thumb"
                android:splitTrack="false" />

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="16dp"
                android:layout_marginTop="-8dp">

                <TextView
                    android:id="@+id/tvCurrentTime"
                    style="@style/TextSmall"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignParentStart="true"
                    android:text="00:00"
                    android:textColor="@android:color/white" />

                <TextView
                    android:id="@+id/tvTotalTime"
                    style="@style/TextSmall"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignParentEnd="true"
                    android:text="00:00"
                    android:textColor="@android:color/white" />
            </RelativeLayout>
        </LinearLayout>

        <ImageView
            android:id="@+id/ivPlayPause"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_play_white"
            android:background="@drawable/shadow_circle"
            android:padding="16dp"
            android:elevation="8dp"
            android:visibility="gone"
            android:contentDescription="@string/play_video" />

        <ProgressBar
            android:id="@+id/pbLoading"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center"
            android:indeterminate="true"
            android:indeterminateTint="@android:color/white"
            android:visibility="gone"
            android:elevation="10dp" />

    </FrameLayout>

</app.olauncher.helper.ZoomableFrameLayout>
